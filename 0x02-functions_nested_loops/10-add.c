#include "main.h"
/**
 * add - returns the sum of two integers.
 * @x: argument x
 * @y: argument y
 * Return: returns the add value of the two arguments
 */
int add(int x, int y)
{
	return (x + y);
}
