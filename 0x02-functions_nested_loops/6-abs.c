#include "main.h"
/**
 * _abs - return absolute of integer.
 *
 * @n: the argument value.
 *
 * Return: returns absolute value.
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
